# Deploy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** Deploy the AI Gallery SPA to Cloudflare Pages with git-based auto-deploy from the existing GitHub repo.

**Architecture:** Cloudflare Pages connects directly to the existing GitHub repo (`emrebardak/AiGalleryWeb`, branch `master`) via OAuth in the Cloudflare dashboard, building with Vite's standard `npm run build` â†’ `dist/` output on every push. No server-side routing or Pages Functions are needed for this deploy â€” those are added later in the add-card sub-project.

**Tech Stack:** Vite 8, Cloudflare Pages (static hosting + git-connected auto-deploy).

## Global Constraints
- Output directory: `dist` (Vite default, already gitignored)
- Build command: `npm run build` (runs `tsc -b && vite build`)
- No custom domain â€” use the default `*.pages.dev` URL
- No environment variables or secrets needed for this deploy
- Production branch: `master` â€” every push triggers a new deploy
- Repo already has `origin` set to `https://github.com/emrebardak/AiGalleryWeb.git`

---

### Task 1: Verify the production build locally

**Files:** none created or modified â€” verification only.

**Interfaces:**
- Consumes: `npm run build` script from [package.json](../../../package.json) (`tsc -b && vite build`)
- Produces: confirmation that `dist/` builds cleanly, so Cloudflare's dashboard build settings can be trusted to match a working local build

- [x] **Step 1: Run the production build**

Run: `npm run build`
Expected: exits `0`. Output ends with a Vite build summary (e.g. `âœ“ built in Xs`) and lists the emitted files under `dist/assets/`. No TypeScript errors from the `tsc -b` step.

- [x] **Step 2: Verify build output exists**

Run: `ls dist && ls dist/assets`
Expected: `dist/index.html` exists; `dist/assets/` contains at least one `.js` and one `.css` file.

- [x] **Step 3: No commit needed**

This task only verifies existing code builds â€” nothing changes. Skip the commit step.

---

### Task 2: Connect Cloudflare Pages to the repo (manual â€” user performs this)

**Files:** none â€” this task happens entirely in the Cloudflare dashboard, not in the repo. It requires an interactive OAuth login tied to the user's own Cloudflare account, which an agent cannot do. Hand this checklist to the user and wait for them to confirm each step.

**Interfaces:**
- Consumes: the verified build from Task 1 (framework preset auto-fills settings that must match Task 1's `npm run build` / `dist` combo)
- Produces: a live `*.pages.dev` URL, needed by Task 3

- [x] **Step 1:** Go to https://dash.cloudflare.com â†’ **Workers & Pages** â†’ **Create** â†’ **Pages** tab â†’ **Connect to Git**
- [x] **Step 2:** Authorize Cloudflare's GitHub App for the `emrebardak/AiGalleryWeb` repo (grant access to just this repo, not all repos, if GitHub offers the choice)
- [x] **Step 3:** Select `emrebardak/AiGalleryWeb` from the list, click **Begin setup**
- [x] **Step 4:** Set **Production branch** to `master`
- [x] **Step 5:** Set **Framework preset** to `Vite`. Confirm it auto-fills:
  - Build command: `npm run build`
  - Build output directory: `dist`

  If either doesn't match, edit it to the values above.
- [x] **Step 6:** Leave **Environment variables** empty, click **Save and Deploy**
- [x] **Step 7:** Wait for the build to finish in the dashboard. Confirm the deploy succeeds and note the assigned `<project-name>.pages.dev` URL â€” needed for Task 3.

---

### Task 3: Record the live URL in the README

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: the URL from Task 2, Step 7
- Produces: nothing consumed by later tasks â€” documentation only

- [x] **Step 1: Add a Live section to the README**

Insert after the intro paragraph (before `## Development`) in `README.md`:

```markdown
## Live

https://<project-name>.pages.dev
```

Replace `<project-name>.pages.dev` with the actual URL from Task 2.

- [x] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add live deploy URL"
```

---

## Self-Review Notes

- **Spec coverage:** Host (Cloudflare Pages) â†’ Task 2. Build config (framework preset/build command/output dir) â†’ Task 1 (local verification) + Task 2 Step 5 (dashboard match). URL (default `*.pages.dev`, no custom domain) â†’ Task 2/3. Env vars (none needed) â†’ Task 2 Step 6. Setup steps from the spec map 1:1 onto Task 2's checklist. "Why Cloudflare Pages over Vercel" and "explicitly out of scope" sections are rationale/exclusions, not actions â€” no task needed.
- **Placeholder scan:** `<project-name>.pages.dev` in Task 3 is an intentional fill-in-after-Task-2 value, not an unresolved placeholder â€” it can't be known until Task 2 runs interactively.
- **Scope:** Deploy only. Pages Functions, `GITHUB_TOKEN` secret, and `functions/api/add-card.ts` belong to the add-card plan, not this one.
